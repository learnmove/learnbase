	<% if @event.errors.any? %>
	<ul>
		<% @event.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
		<% end %>
	</ul>
		
	<% end %>
	<%= f.text_field :name %>
	<%= f.label :description,"description" %>
	<%= f.text_area :description %>